/**
 * Global variables definition and any codes modify the `global` object are defined here.
 */
'use strict';

// side-effect
require('should');
require('should-sinon');

module.exports = {
    TEST_ROOT: __dirname,
    sinon: require('sinon'),
};
